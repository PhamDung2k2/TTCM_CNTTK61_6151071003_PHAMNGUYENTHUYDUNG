@model IEnumerable<QLNS.Models.TTChamCong>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2 class="text-center">Danh Sách Chấm Công</h2>

       @* <div class="form-group row">
            <div class="col-md-1">
                <a href="@Url.Action("Create", "TTChamCong")" class="btn btn-outline-success">Thêm mới</a>
            </div>
        </div>*@

<table class="table table-hover text-center ">
    <tr>
        <th class="text-center">
            @Html.DisplayNameFor(model => model.NgayCC)
        </th>
        <th class="text-center">
            @Html.DisplayNameFor(model => model.TVao)
        </th>
        <th class="text-center">
            @Html.DisplayNameFor(model => model.TRa)
        </th>
        <th class="text-center">
            @Html.DisplayNameFor(model => model.ViPham)
        </th>
        <th class="text-center">
            @Html.DisplayNameFor(model => model.NhanVien.HoTen)
        </th>
        <th class="text-center">Thao Tác</th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NgayCC)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TVao)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TRa)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ViPham)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NhanVien.HoTen)
            </td>
            <td>
                @*<a href="@Url.Action("Details", "TTChamCong", new { id = item.IdCC })" class="btn btn-success btn-sm" data-toggle="tooltip" data-placement="bottom" title="Chi Tiết"><i class="fa fa-book"></i></a>
                *@<a href="@Url.Action("Edit", "TTChamCong", new { id = item.IdCC })" class="btn btn-primary btn-sm" data-toggle="tooltip" data-placement="bottom" title="Chỉnh Sửa"><i class="fas fa-pen"></i></a>
                <a href="@Url.Action("Delete", "TTChamCong", new { id = item.IdCC })" class="btn btn-danger btn-sm" data-toggle="tooltip" data-placement="bottom" title="Xóa"><i class="far fa-trash-alt"></i></a>

            </td>
        </tr>
    }

</table>
